# =====================================================================
# Python dependencies for the Finance Analytics Platform
#
# Install:   pip install -r requirements.txt
# Python:    3.10 or higher (tested on 3.10, 3.11, 3.12)
#
# Notes:
#   * data/generate_sample_data.py uses only the Python standard library
#     (csv, random, datetime, pathlib) — no pip install needed for it.
#   * Everything below is for the dbt + Snowflake workflow.
# =====================================================================

# --- Core: dbt + Snowflake adapter ---
# Pin to minor version. Patch updates safe; minor may shift macro behaviour
# from dbt_utils / dbt_expectations.
dbt-core==1.8.7
dbt-snowflake==1.8.4

# --- Snowflake direct connectivity ---
# Only needed if you want to run snowflake/01_setup/03_bootstrap_load.sql
# from Python (e.g. a small loader script) instead of the Snowsight worksheet
# or SnowSQL. Keep it in — small download, frequently useful.
snowflake-connector-python==3.12.4

# --- Lint & formatting (recommended; matches the CI sketch in
#     bitbucket/BRANCHING_STRATEGY.md) ---
sqlfluff==3.2.5
sqlfluff-templater-dbt==3.2.5
yamllint==1.35.1
